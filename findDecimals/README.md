<h1>Find the prime number of an integer   and return it as an array</h1>

```javascript
import { findDecimals } from "find-decimals";

console.log(findDecimals(10));

// [...prime number]
```
